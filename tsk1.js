/**
 * Write a program that generates a random integer between 1 and 100 using Math.random and Math.floor. Print the random number to the console.
 */

const number = Math.floor(Math.random()*100)+1;
console.log(number);