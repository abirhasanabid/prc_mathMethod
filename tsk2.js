/**
 * Write a program that calculates the square of a given number. For example, if the input is 5, the output should be 25. Use Math.pow for this.
 */

const squre = Math.pow(5,2);
console.log(squre);